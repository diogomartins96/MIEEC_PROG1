#include <stdio.h>

main()
{
 float r, a, p, pi;
 pi=3.1416;
 printf("Digite o raio, r: \n");
 scanf("%f", &r);
 a= pi*r*r;
 p= 2*pi*r;
 printf("A área é de: %f\n", a);
 printf("O perimetro é de: %f\n", p);
 return 0;
}
 
