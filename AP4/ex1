#include <stdio.h>
main()
{
   int num, soma=0;
	printf("Insira um número inteiro para calcular a soma dos seus dígitos:");
	scanf("%d", &num);
	while (num>=10)
	{
		soma+=num%10;
		num/=10;
	}
	soma+=num;

	printf("A soma dos dígitos é %d\n", soma);
}
